# =============================================================================
# Package Allowlist
# =============================================================================
#
# Packages listed here are pre-approved for installation.
# When PACKAGE_STRICT=1, only packages on this list can be installed.
# When PACKAGE_STRICT=0 (default), unlisted packages trigger a warning
# but are not blocked.
#
# Format:
#   package              — any version allowed
#   package==1.2.3       — specific version only
#   package>=1.2.0       — version 1.2.0 or later (loose check)
#   # comment
#
# Maintain this file centrally and distribute via config management.
# =============================================================================

# ── Python / PyPI ────────────────────────────────────────────────────────────

# Web frameworks
flask
fastapi
uvicorn
django
starlette

# HTTP / networking
requests
httpx
aiohttp
urllib3

# Data / science
numpy
pandas
scipy
matplotlib
pillow

# Database
sqlalchemy
psycopg2-binary
pymongo
redis

# Serialization / config
pyyaml
toml
pydantic
pydantic-settings

# CLI / DevOps
click
typer
rich
paramiko

# Testing
pytest
pytest-cov
pytest-asyncio
coverage

# Utilities
python-dateutil
pytz
jinja2
markupsafe
cryptography
bcrypt

# AI / ML (pin specific versions — high-value targets)
openai==1.82.0
anthropic==0.52.0
langchain>=0.3.0
transformers>=4.40.0

# NOTE: litellm intentionally omitted — compromised in TeamPCP campaign
# NOTE: telnyx intentionally omitted — compromised in TeamPCP campaign

# ── npm ──────────────────────────────────────────────────────────────────────

# (Same file can be used for npm if the verification script is extended)
# express
# react
# typescript
# eslint
